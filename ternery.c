#include<stdio.h>
int main()
{
  int num;
  printf("Enter your number:");
  scanf("%d",&num);
  (num<7) ? printf("num is less than seven") : printf("num is greater than seven");


  return 0;
}
